# The bundle layer this package installs into a profile: one row whose entry id
# is the settings namespace the browser card reads (`TASK_ALERT_SETTINGS_NAMESPACE`).
- insert:
    - id: ui-task-alert
      name: dsh-ui-task-notify
